# Differential Baseline Format

Status: accepted Phase 0 format.
Date: 2026-06-24.

Baseline records compare backend output without committing large image artifacts
by default. Records are JSON so they can be generated by Rust, shell tools, or
future Node wrappers without a custom parser.

## Location

- Small reviewed examples: `baselines/examples/`
- Large local artifacts: `baselines/artifacts/` (ignored by Git)

## Schema

Each record is one JSON object:

```json
{
  "schema_version": 1,
  "fixture": {
    "id": "text-page",
    "path": "fixtures/generated/text-page.pdf",
    "sha256": "c88d9b13ae24f10969f32db2afdebac3587654c5767c831e37ecc49df0b5650d"
  },
  "backend": {
    "id": "pdfium",
    "revision": "573758fe2dd928279cd52b5a4bc955a6938aab39",
    "configuration": "pdfrust-thumb"
  },
  "request": {
    "page_index": 0,
    "max_edge": 256,
    "background": "#ffffffff",
    "output_format": "rgba"
  },
  "result": {
    "status": "success",
    "dimensions": {
      "width": 256,
      "height": 137,
      "stride": 1024,
      "pixel_format": "rgba8"
    },
    "pixel_sha256": "replace-after-local-render",
    "artifact_path": "baselines/artifacts/text-page-pdfium-256.rgba"
  },
  "tolerance": {
    "policy": "exact-for-pdfium-baseline",
    "max_changed_pixels": 0,
    "max_channel_delta": 0
  }
}
```

Failure records use the same fixture, backend, and request sections but replace
`dimensions`, `pixel_sha256`, and `artifact_path` with an error class:

```json
{
  "result": {
    "status": "error",
    "error_class": "malformed"
  }
}
```

The format is backend-neutral: `backend.id` may later be `rust-native`,
`poppler-reference`, or another comparison renderer.
